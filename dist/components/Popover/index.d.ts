import React from 'react';
import { popoverType } from '../../types/popover';
export default function Popover({ popoverRef, closePopover, colors, dataProvider, locale, onComplete, value, }: popoverType): React.JSX.Element;
