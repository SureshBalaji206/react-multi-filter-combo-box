import React from 'react';
import { MultiSelectPropType } from '../../../types/popover';
export default function MultiSelect({ cancelText, closePopover, onComplete, options, popoverRef, primaryColor, selectAllText, submitText, }: MultiSelectPropType): React.JSX.Element;
