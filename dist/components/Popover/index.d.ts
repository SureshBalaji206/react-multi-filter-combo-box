import React from 'react';
import { popoverType } from '../../types/popover';
export default function Popover({ popoverRef, closePopover, dataProvider, locale, onComplete, value, }: popoverType): React.JSX.Element;
